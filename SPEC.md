# 黒猫アスレチック（仮名）仕様書

> 本書は Claude Code が実装に着手するための仕様書である。
> **「未決事項」に挙げた項目は勝手に決めず、実装前に作者へ確認すること。**
> プロジェクト名は仮名。正式名称は仕様がまとまった段階で決定する。

---

## 1. コンセプト

「猫ならどう進める？」を考える、**脱出ゲーム寄りの3Dアスレチック**。

- プレイヤーは黒猫を操作し、人間向けの空間を猫の身体能力で攻略する。
- 遠目にはヒントを出さない。**近づいて触って初めて分かる**発見が遊びの中心。
- 構造は「仕掛けを見つける（探索）→ 仕掛けを動かして走る（疾走）」の二段構成。
- 身内向けの自作ゲーム。ヒント機構は持たない（ヒントは作者に直接聞く）。
- 核心：**猫を人間のように操作するのではなく、猫として空間を攻略する。**

---

## 2. 対応環境

| 項目 | 内容 |
|---|---|
| PC | Webブラウザ（キーボード＋マウス） |
| スマートフォン | Webブラウザ（タッチ操作）、**横向き固定** |
| 配布 | PWA（ホーム画面追加可） |
| 公開 | GitHub Pages |
| 言語 | 日本語（UI・テキストすべて） |

---

## 3. 技術スタック

| 層 | 採用 | 備考 |
|---|---|---|
| 描画 | Three.js | ゲームエンジンは使わない |
| 言語 | TypeScript | |
| ビルド | Vite | |
| 物理・当たり判定 | Rapier（`@dimforge/rapier3d-compat`） | WASMをJSに埋め込んだ版を使う（後述） |
| PWA | Vite用PWAプラグイン等 | マニフェスト＋Service Worker |
| 3D素材（後期） | Blender自作＋CC0フリー素材 | 初期はプリミティブのみ |

### 3.1 猫の制御方式

- 猫は**物理ボディにしない**。**キネマティック・キャラクターコントローラー**（Rapier のキャラクターコントローラー機能）で制御する。
- 移動・ジャンプ・落下・爪アクションは手続き的に制御する。
- 物理シミュレーションは背景（揺れる物など）に限定し、必要な場合のみ使う。

```
プレイヤー入力
  ↓
キャラクターコントローラー（移動・ジャンプ・落下）
  ↓
爪アクション（手続き的に制御）
  ↓
Rapier（地面・壁・足場との衝突判定）
```

### 3.2 見た目（画風）

- 参考画像：[docs/art/reference-stages.png](docs/art/reference-stages.png)（**雰囲気の参考**。画像内の UI・ルール・ステージ順は採用しない。それらは本仕様書に従う）
- **線画＋フラット塗りのイラスト調**。はっきりした輪郭線、2〜3段階の陰影、暖かい色合い。
- 3D では**トゥーン表現（段階陰影＋輪郭線）**で再現する。
- 空間は**人間の実寸**で作り、猫が小さく見える構図にする。
- 質感（木目・錆・布など）は写実的な質感表現ではなく、塗り分けと描き込みで表す。

### 3.3 猫の姿

- **デフォルメしない四本脚の猫**。体を水平にした四足の姿勢（マインクラフトの猫のような姿勢）。
- 見た目は 3.2 の画風に合わせる。
- 当たり判定は**体の形に合わせた角丸の箱**（幅 0.14 × 高さ 0.26 × 長さ 0.44 m）で、猫の向きと一緒に回る。
  - 向きを変える前に回した形がぶつからないか調べ、ぶつかる場合は回さない（狭い所では向きを保ったまま後ずさり・横歩きで動く）。
  - 0.1m 程度の段差は歩いて乗り越える（Rapier の自動乗り越えは使わず自前で判定）。

---

## 4. 操作仕様

### PC

| 操作 | 入力 |
|---|---|
| 移動 | W / A / S / D |
| 視点 | マウス（ポインターロック） |
| ジャンプ | Space |
| 爪 | Shift |

### スマートフォン（横向き）

| 操作 | 入力 |
|---|---|
| 移動 | 画面左側の仮想ジョイスティック |
| 視点 | 画面右側のスワイプ |
| ジャンプ | 画面内ジャンプボタン |
| 爪 | 画面内爪ボタン |

- ボタン類はゲーム画面を大きく隠さない配置とする。
- 操作数は増やさない方針。

---

## 5. カメラ

- 三人称視点。猫を後方から見る。
- プレイヤー操作で視点を回転できる。
- **壁・狭所でのカメラめり込み対策は必須**（本作は狭所通過を扱うため）。具体的手法はプロトタイプで検証して決める。
- **プレイ用カメラと導入演出用カメラは別系統**で実装する。

---

## 6. 爪ボタン仕様（最重要）

### 6.1 定義

爪ボタンは、**対象オブジェクト固有のアクションを発生させる汎用インタラクション操作**とする。
（一般的なゲームのAボタンと同じ考え方。対象によって起きることが変わる）

対象は移動系に限定せず、環境操作・ギミック・その他を含む。ただしゲームの中心は移動。

```
爪ボタン
├─ 移動系（例）
│  ├─ カーテンを登る
│  ├─ ツタを登る
│  └─ 金網をよじ登る
└─ 環境操作系（例）
   ├─ ドアを開ける／閉める
   ├─ 引き出しを開ける／閉める
   ├─ スイッチを押す
   └─ 箱を動かす
```

### 6.2 発見性

- **遠目のヒント・ハイライト・アイコン表示は一切出さない。**
- 近づいて爪を出して初めて分かる。
- **近接時の反応は必ず返す。** 対象でない物に爪を出した場合も、何らかの一貫した反応（音・しぐさ等）を返す。同じ見た目の物が場合によって反応したりしなかったりしないこと。
- 反応表現：**前脚で引っかく仕草＋触れた面に爪痕（しばらくして消える）**。対象かどうかに関係なく毎回同じ反応を返し、対象ならさらにその動作が起きる。音はサウンド仕様の決定後に追加する。

### 6.3 実装方針

- 爪の対象は「種類」と「動作」を持つ**インタラクタブル**として共通化する。
- ステージ側は**配置データ**で対象を管理する。新しい対象の追加 ＝ 新しい動作を1つ追加するだけで済む構造にする。
- 複数の対象が近い場合の選択は、**近接距離＋向き（猫・カメラの向き）で裏で判定**する。画面上に選択候補は表示しない。
- 動作には「押した瞬間に1回で完了する物（ドア等）」と「押している間続く物（登る等）」がある前提で設計する。

### 6.4 登る物（押している間続く動作）の操作

- 爪を押している間、面に張り付く。ジョイスティックの上下で登り降り、左右で横移動。
- 爪を離すとその場から落ちる。
- **ジャンプボタンで後ろへ飛び降りる。**
- 上の縁まで登ると自動で乗り越える（上に乗れる面があれば乗り、薄い金網なら向こう側へ降りる）。
- 下まで降りて床に着いたら、自動で四つ足に戻る。

---

## 7. 落下・状態の扱い

| 項目 | 仕様 |
|---|---|
| 落下ダメージ | **なし**。何事もなかったように着地する（猫のリアル） |
| リスポーン | **なし**。落ちたら歩いてスタート地点側へ戻る |
| チェックポイント | **作らない**（難易度を見て後日判断） |
| ギミックの状態 | 常に保持される（リセットの概念がない） |
| ギミックの可逆性 | **全ギミック可逆**（開けたら閉められる、動かしたら戻せる） |

### 設計上の要件

- 戻る距離が長くならないよう、ステージは**縦移動多め・広さに制限**を持たせる。
- 落下先の地面層も探索可能な空間として扱う（落下＝ただの罰にしない）。
- 一方通行ギミック（壊れる・燃える・割れる等）は原則作らない。
- **箱の移動は物理ではなく、決まった位置間のスライド**（手続き的ギミック）とする。可逆・安定・詰み防止のため。
- ギミックは**可逆前提の状態機械**として実装する。

---

## 8. ステージ構成（プレイ順・固定）

| 順 | ステージ | ゴール | 位置づけ |
|---|---|---|---|
| 1 | 🌲 工場の裏の森 | 木の上を伝い、フェンスを越えた先の「ご飯皿と、傘が差してありふかふかの布が敷かれた段ボール」 | チュートリアル |
| 2 | 🏭 工場 → 事務所 | 事務所のPCのキーボード（上で寝る） | 本編 |
| 3 | 🏠 家（吹き抜けのある住宅） | ふかふかの猫ベッド | 締め |

- 物語線：野良（森）→ 工場に侵入 → 事務所 → 家（飼い猫へ）。
- ステージ間の繋ぎ演出は**当面入れない**（後から追加可能な構造にだけしておく）。

### 8.1 各ステージの想定環境

- **森**：木の幹、太い枝、細い枝、ツタ、岩、フェンス（**板塀**：登れない。作者判断で金網から変更）、段ボール、傘、ご飯皿
- **工場/事務所**：コンベア、配管、鉄骨、金網、梯子、機械、高所作業場、事務所への侵入経路（ドア・窓・通気口等）、事務机、PC
- **家**：吹き抜け、階段、手すり、ソファ、家具、棚、カーテン、梁

### 8.2 森の構成（グレーボックス・第3版で決定）

- 板塀（2.4m、登れない）の向こうのゴールへ、**越え方は少なくとも3通り**（人によって通る道が違う）：
  - A. ツタの木（スタート正面）：ツタ → ①の股(2.1) → 枝の先から②の股(2.6)へ跳ぶ → 枝 → ③の木(3.3) → 細い枝（幅 0.20） → 葉の塊(3.85) → 葉の上を渡って塀を越える
  - B. 倒木（右手）：倒木 → 岩(1.3) → ④の枝(1.95) → 枝先から⑤の枝(2.4)へ跳ぶ → 枝先から塀を跳び越える（歩いて落ちると塀に当たる）
  - C. 岩（左手）：岩を4つ跳び移って高くなる(0.56 → 1.1 → 1.7 → 2.1) → 塀の上(2.4)に跳び乗り、向こうへ降りる
- **道を目立たせない**：木・岩・倒木を多く置き、行き止まりの枝・岩・倒木も混ぜる。遠目にはどれが道か分からないようにする。
- 見た目が一本道にならないよう、幹は丸く、枝は斜め、葉や岩は丸い塊で置く（配置データで円柱・傾いた板・丸い塊を使える）。
- 難易度の目安：塀の向こうへ行くのに**最短でも5回の移動**（歩き・跳び・登りの合計）が要る。各ジャンプは届く限界に近い距離にする。
- 地面に置く岩はすそを広くし、下をえぐらない（ふくらみの下に潜ると跳べなくなるため）。
- **幹は登れない**（爪痕のみ）。登れるのはツタだけ（同じ見た目の物は同じ反応）。ツタは幹の表面にかかる「登れる範囲」で、段差を作らない。
- 板塀は登れない。地面から跳んで届く高さ（1.0m）より、木の股・枝・葉はすべて高い。
- ゴール：段ボールに爪 → 猫が中で丸くなり「クリア」表示と「もう一度」。ご飯皿は爪で揺れるだけ。
- 配置データ：`src/stages/forest/forestData.ts`。検算（`npm run verify:course`）では、寸法・支え・傾きに加えて、**どこからどこへ跳べるかをたどって到達できる場所を全部調べる**（近道・樹冠に乗れる等を検出し、塀の向こうまでの最短移動回数も出す）。実際に通れるかは開発時のルート自動テスト（ブラウザのコンソールで `runRouteTests()`）で確かめる。

### 8.3 ステージごとの留意点

- **森（チュートリアル）**：ヒントは出さないが、**最初の爪の対象は必ず目に入る位置**に置き、最初の1回の体験を保証する。
- **森のゴール**：ご飯皿と段ボールが隣接するため、クリア対象を一意にする（ご飯皿は「反応するがクリアではない」等）。
- **工場/事務所**：同一ステージで規模が最大。スマホの描画負荷に最も注意。
- **工場のゴール演出**：導入演出では「事務所の机あたり」までを見せ、**キーボードで寝るという落ちは到着して初めて分かる**ようにする。

---

## 9. クリア条件と導入演出

### 9.1 クリア

- 各ステージのゴール対象に**爪ボタンでアクション**するとクリア。
- 着いただけでは自動クリアしない。

### 9.2 ステージ開始演出

```
ゴールを映す → ズームアウトしてコース全体をある程度見せる → 猫へズームイン → そのままプレイ視点へ
```

- 見せるのは「ゴールの位置」であり、「経路」や仕掛けの位置が漏れすぎない構図にする。
- **スキップ可能**にする（初回は全編、以降はスキップ／短縮可）。
- ズームアウト時は描画負荷のピーク。グレーボックス段階から実機で確認する。

---

## 10. 開発順（プレイ順とは別）

| 段階 | 内容 | 備考 |
|---|---|---|
| ① 最小プロトタイプ | 箱の世界＋猫カプセル。WASD、マウス視点、ジャンプ、落下、衝突、スマホのジョイスティック＋スワイプ視点、カメラめり込み対策の検証 | PC＋実機スマホで確認 |
| ② 爪 | インタラクタブルの仕組み＋最小1〜2種の対象（例：登る物、開閉する物） | |
| ~~③ 家グレーボックス~~ | ~~吹き抜け住宅を箱だけで組み、基本システムを検証~~ | **省略**（作者判断：早くステージ制作に入るため） |
| ⑤ 森 | Stage 1 制作。まず箱だけで形を作り、その後に見た目を付ける | ② の直後に着手 |
| ④ 面白さの検証 | 身内（猫を飼っている人・飼っていない人の両方）に遊ばせる | **森ができた時点で実施** |
| ⑥ 工場/事務所 | Stage 2 制作 | |
| ⑦ 家（本番） | Stage 3 制作 | |
| ⑧ 素材差し替え | グレーボックス → CC0素材 → 必要な物のみ自作 | |

**PWA・実機スマホ確認は①から行う**（最後に対応しない）。

---

## 11. 実装上の注意（事前に必要な設定）

デフォルト設定のままでは動かない、または後から詰まる箇所。

1. **Rapier は `@dimforge/rapier3d-compat` を使う。**
   WASMをbase64でJSに埋め込んだ版で、Viteの本番ビルドやService Workerのキャッシュで `.wasm` ファイルの扱いに悩まずに済む。利用前に `await RAPIER.init()` が必要。
   通常版（`@dimforge/rapier3d`）を使う場合は、Viteの WASM 読み込み設定と、Service Worker のキャッシュ対象に `.wasm` を含める設定が別途必要になる。
2. **GitHub Pages はサブパス配信**（`https://<user>.github.io/<リポジトリ名>/`）。
   Vite の `base`、PWAマニフェストの `start_url` / `scope`、Service Worker のスコープを揃えること。
3. **iOS Safari はポインターロック非対応**。PC入力（ポインターロック）とタッチ入力は分岐して実装する。
4. **横向き固定**：Screen Orientation API によるロックはブラウザ・OSで対応差がある。
   マニフェストで `orientation: landscape` を指定しつつ、**縦向き時は「端末を横にしてください」オーバーレイを表示**する方式を併用する。
5. **iOS のメモリ制限**：タブが落ちやすい。テクスチャ・モデルのサイズを早期から計測する。
6. **モバイル性能予算**：ポリゴン数・テクスチャ容量・ドローコール数・メモリ使用量を①の段階から計測できるようにしておく（デバッグ表示等）。

---

## 12. 開発規約

- コード内コメントは**日本語**。
- 命名は TypeScript の一般的な規約（変数・関数は camelCase、クラス・型は PascalCase）。
- 3D／幾何を扱うコードは、提示前に**寸法・高さ・接地の整合を数値で検算**すること。
  特に「軸の取り違え」「支持の欠落（宙に浮く）」「地面高さと原点のズレ」を確認する。
- 検証用コード（グレーボックス）と本番コードは分離する。

---

## 13. 未決事項（勝手に決めず作者に確認）

| 項目 | 状態 |
|---|---|
| 猫の移動速度・ジャンプ力・空中操作 | 初期値：速度 3.0m/s・ジャンプ高さ 1.0m・重力 20m/s²・空中操作 30%（プロトタイプで調整継続） |
| カメラめり込み対策の手法 | **決定**：注視点から球を飛ばして（castShape）障害物の手前にカメラを置く＋近すぎる時は猫を半透明→非表示 |
| 爪の近接反応の表現（音・しぐさ・爪痕等） | **決定**：引っかく仕草＋爪痕（6.2）。音はサウンド決定後 |
| 爪の対象カタログ（ステージごとの具体的な仕掛け一覧） | 各ステージ制作と並行で決める（家グレーボックスは省略） |
| チュートリアルでの爪の最初の体験の設計 | 森制作時に決定 |
| チェックポイントの要否 | 難易度を見て後日判断 |
| ステージの具体的な構造・寸法 | 森：8.2 で決定（グレーボックス）。工場・家は未決 |
| 敵・障害物の有無 | 未決 |
| タイムアタック・スコア | **なし**（タイマー・スコア表示も持たない） |
| セーブ機能 | 未決 |
| サウンド | 未決 |
| グラフィック仕様（画風） | **決定**：イラスト調（トゥーン表現）。3.2 を参照 |
| オフライン対応範囲 | 当面：ビルド成果物をすべてプリキャッシュ（一度開けばオフライン起動可） |
| ステージ間の繋ぎ演出 | 当面なし（後で追加検討） |
| 正式なゲームタイトル | 未決（仮名で進行） |
