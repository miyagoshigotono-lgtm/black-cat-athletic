# Claude Code への初回指示

リポジトリ直下の `SPEC.md` を最初に全部読んでください。

今回の作業範囲は **SPEC.md「10. 開発順」の ① 最小プロトタイプのみ** です。②以降には手を付けないでください。

## ①で作るもの

- Vite + TypeScript + Three.js + `@dimforge/rapier3d-compat` のプロジェクト雛形
- 箱（プリミティブ）だけで組んだ検証用の足場群（高低差・狭い隙間・壁を含む）
- 猫カプセル（キネマティック・キャラクターコントローラー）
  - 移動、ジャンプ、落下、衝突
- 三人称カメラ
  - PC：マウス視点（ポインターロック）
  - スマホ：右側スワイプで視点
  - 壁・狭所でのめり込み対策（手法の候補を挙げ、採用案を作者に確認してから実装）
- スマホ操作：左側仮想ジョイスティック、ジャンプボタン（爪ボタンは配置のみ・動作なし）
- 横向き固定（縦向き時は「端末を横にしてください」を表示）
- PWA化（マニフェスト＋Service Worker）
- GitHub Pages へのデプロイ設定（サブパス対応：`base` / `start_url` / `scope`）
- 性能計測の簡易デバッグ表示（FPS、ドローコール数、三角形数）

## 守ること

- SPEC.md「13. 未決事項」は勝手に決めず、必要になったら作者に質問する。
- コード内コメントは日本語。
- 3D配置は、寸法・高さ・接地を数値で検算してから提示する。
- 最初に実装方針（ファイル構成・主要クラス・使うライブラリのバージョン）を提示し、作者の合意を得てからコードを書く。

## 完了条件

- PC ブラウザと実機スマホ（iOS Safari / Android Chrome）の両方で、猫カプセルが箱の世界を走り・跳び・落ち、カメラが壁にめり込まないこと。
- GitHub Pages 上で PWA としてホーム画面に追加でき、起動できること。
